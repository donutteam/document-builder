{
  "name": "@donutteam/document-builder",
  "version": "5.2.1",
  "description": "A collection of classes that make it easy to build complex HTML strings entirely with JavaScript.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "npx rimraf lib && npx tsc",
    "lint": "eslint . -c ./.eslintrc.cjs",
    "lint-fix": "eslint . -c ./.eslintrc.cjs --fix",
    "prepublishOnly": "npm run build",
    "test": "npm run build && mocha ./tests/**/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/donutteam/document-builder.git"
  },
  "keywords": [
    "html",
    "components"
  ],
  "author": "Donut Team",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "entities": "^4.4.0"
  },
  "devDependencies": {
    "@donutteam/eslint-config": "^2.0.0",
    "eslint": "^8.36.0",
    "mocha": "^10.2.0",
    "rimraf": "^4.4.0"
  }
}
